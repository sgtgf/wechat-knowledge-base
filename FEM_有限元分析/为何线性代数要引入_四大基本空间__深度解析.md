# 为何线性代数要引入“四大基本空间”？深度解析


> 原文地址: [https://mp.weixin.qq.com/s/\_hBdSmTy0z8AnIJwtlE0ZQ](https://mp.weixin.qq.com/s/_hBdSmTy0z8AnIJwtlE0ZQ)

![图片](https://mmbiz.qpic.cn/sz_mmbiz_gif/icVUVTFlBCVXkszTAU2XWHdU0FE9cibs9dt9GsJA7gZkOA8jojfUlONf2TTgU5VctDMoxjpdxwvicBCg02E4qIu6Q/640?wx_fmt=gif&from=appmsg&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1)

![计算机生成了可选文字:
且2亠0](https://mmbiz.qpic.cn/sz_mmbiz_png/o7bJfzdLv3eqHFSuly9e9dh4N5lpib4AdPwtXCOrGOObrj8nZe7sZ83MEwuRyCTd8AyRebNuSB24wZyudibR4uABMNCiadRwKrNj51cwlgusrM/640?wx_fmt=png&from=appmsg)  

在学习线性代数的过程中，我们总会遇到一个看似抽象却又无比核心的概念——矩阵的“四大基本空间”：行空间、列空间、零空间，以及左零空间。

很多初学者看到这四个名词，第一反应往往是：这又是为了凑概念而凑出来的吗？为什么要定义这四个空间？它们之间到底有什么联系？

今天，我们不从教科书式的定义出发，而是从一个最根本的问题出发，来重新理解这四个空间的“存在意义”。

这个问题就是：。

是的，整个线性代数，尤其是应用层面，几乎都是围绕这个方程展开的。它本质上是在问：**哪些输入 ，经过矩阵  的作用以后，会变成输出 ？**

所以，我们不要先背“行空间、列空间、零空间、左零空间”，而是从  本身出发，看看这四个空间是如何自然浮现出来的。

* * *

## 把  看成一个“机器”

我们可以把矩阵  想象成一个“机器”或者“黑箱”。它接收一个输入向量 ，然后输出一个向量 。

用数学语言表达就是：

如果  是一个  的矩阵，那么它把一个  维的输入变成一个  维的输出：

但这还只是外壳。更重要的是，这个机器  并不是万能的。它**不一定能完整保留输入的所有信息**，也不一定**能产生所有可能的输出**。

于是，自然就引出了两个核心问题：

1.  **输入侧的问题**：在输入空间里，哪些方向被  保留下来了？哪些方向被  彻底“消掉”了？
    
2.  **输出侧的问题**：在输出空间里，哪些结果是  能产生的？哪些结果是  永远也产生不了的？
    

这四个基本空间，其实就是用来回答这两个问题的。

* * *

## 为什么要用  定义“零空间”？

零空间（Null Space）往往是最容易被误解的概念。很多人觉得，定义  是为了“凑”一个齐次方程。

其实不然。零空间回答的是一个非常物理的问题：**哪些输入被矩阵  完全“消掉”了？**

也就是解这个方程：

这句话的意思不是“我们随便令右边等于 ”，而是在问：**有没有一些非零的输入 ，经过  作用后，输出变成了 ？**

如果有的话，说明这些输入方向对输出完全没有贡献，被“抹杀”了。

举个直观的例子：假设  是一个把三维空间投影到地面的矩阵。那么，竖直方向的向量（比如 ）经过投影后，就变成了 。这个“竖直方向”，就是零空间。

所以，零空间的定义是：

它表示：**输入空间中，被  抹掉的方向**。

这就是为什么零空间属于“输入侧”——因为它讨论的是“什么样的输入  会被消掉”。

* * *

## 为什么“行空间”也是输入侧的？

这是最容易让人困惑的地方。

行空间（Row Space）确实是由  的行向量张成的，而且每一行有  个元素，所以它在  里。但这只是形式上的解释。

更直观的理解是： 的计算过程，本质上是  的每一行分别和  做点积。

比如：

那么：

每一个输出分量，都是“某一行”与“输入 ”的点积。

这意味着，矩阵  是通过它的“行”去“检测”输入  的。

你可以把行向量想象成一组“探测器”：

它们在问：**输入  在这些行向量方向上有没有“投影”？有没有“成分”？**

所以，行空间的真正意义是：

> **输入空间中，能被  “检测到”、“看见”的方向。**

而零空间是：

> **输入空间中，完全“检测不到”、“看不见”的方向。**

这两个空间是互相垂直的：

并且，整个输入空间  可以分解为这两个空间的直和：

也就是说，任何一个输入 ，都可以分解为两部分：

其中：

-    在行空间里，能被  看见；
    
-    在零空间里，会被  忽略。
    

![](https://mmbiz.qpic.cn/sz_mmbiz_png/o7bJfzdLv3dichjjice0OW7pjmuR5o7O6ciaOlpmznexSfUnq4qlXjmiax3ib9z6fYtoPPGrOmFhQpeHia8d2HvC5nEOHoIyRzY0lOT1AcyYZwlaU/640?wx_fmt=png&from=appmsg)

代入 ：

**这句话非常重要：输入  中，只有行空间部分会影响输出；零空间部分完全不影响输出。**

所以，行空间不是因为“行有  个数”才被硬塞进输入空间，而是因为：**它代表了输入中真正能被  看见、检测、利用的部分。**

* * *

## 为什么“列空间”是输出侧的？

现在我们来看输出侧。

 还有一种展开方式：按列展开。

假设  的列向量是 ，输入 ，那么：

这说明： 永远是  的列向量的线性组合。

所以，所有可能的输出 ，只能落在由这些列向量张成的空间里。这个空间就是**列空间（Column Space）**。

因此，列空间的物理意义是：

>  **能够制造出来的所有输出。**

这直接对应到方程  的解的存在性。

方程有解的条件就是：

也就是说，右端项  必须是  的列向量能够“拼”出来的结果。如果  不在列空间中，那么无论你怎么选输入 ，都不可能得到这个 。

* * *

## 为什么会有“左零空间”？

列空间回答的是：**哪些输出能被  做出来？**

那自然还有一个反面问题：

> **输出空间里，哪些方向是  永远到不了的？**

这就是**左零空间（Left Null Space）**。

左零空间定义为：

这等价于：

这意味着什么？

如果  在左零空间里，那么对于任意输入 ，都有：

也就是说：

对所有能够由  产生的  都成立。

所以，左零空间中的向量 ，和所有列空间中的向量都垂直。

因此：

并且：

直观地说：

-   **列空间**：能到达的输出方向。
    
-   **左零空间**：永远到不了的输出方向。
    

* * *

## 四个空间如何共同解释 ？

![dimension 
row 
space 
of A 
90 0 
nullspace 
of A 
dimension 
row space to column space 
dimension 
column 
space 
of A 
nullspace to 0 
900 
nullspace 
of AT 
dimension 
= m—r 
Figure 4.2: Two pairs of orthogonal subspaces. The dimensions add to n and add to m. 
This is the Big Picture—two subspaces in RTE and two subspaces in Rm](https://mmbiz.qpic.cn/mmbiz_jpg/o7bJfzdLv3eLfJWqG6dBia4D6mhYFuTcA8ZsIicrDMVyHRPJdU0o4BkR3fYGUj1FibQ06k7GB9dav76un7XXRj25icyLSCYjgbDibWawbmlkial9s/640?wx_fmt=jpeg)  

现在，我们把这四个空间全部放回  这个方程中，看看它们是如何协同工作的。

**输入侧：**

输入  可以分解为：

其中：

代入 ：

所以：

> **行空间部分决定输出，零空间部分不影响输出。**

这解释了方程的**解的结构**。

如果  有一个特解 ，那么所有解就是：

意思是：你找到了一个解，还可以随便加上零空间里的向量，因为它不会改变输出。

**输出侧：**

输出 ，但  不一定能覆盖整个 ，它只能落在列空间：

所以，方程  有解的前提是：

如果  不在列空间中，说明它含有一些  永远无法产生的方向，也就是左零空间方向上的成分。

所以：

> **列空间决定  是否可达；左零空间描述  中不可达的部分。**

* * *

## 用一句话串起四大基本空间

对于方程 ，我们可以这样一句话总结：

-   **行空间**：输入中能被  看见的部分。
    
-   **零空间**：输入中被  消掉的部分。
    
-   **列空间**：输出中  能到达的部分。
    
-   **左零空间**：输出中  永远到不了的部分。
    

所以，它们不是孤立定义的，而是共同描述了矩阵  这个线性变换：

输入空间输出空间

在输入侧， 把输入分成了：

-   **有用的输入方向**（行空间）
    
-   **无效的输入方向**（零空间）
    

在输出侧， 把输出空间分成了：

-   **能产生的输出方向**（列空间）
    
-   **不能产生的输出方向**（左零空间）
    

* * *

## 直观的小例子

我们来看一个简单的例子。

设：

那么：

这个矩阵的作用很简单：。它只读取输入的第一个分量，完全忽略第二个分量。

所以：

-    方向能被看见。
    
-    方向会被消掉。
    

因此：

-   ：输入中能被检测到的方向。
    
-   ：输入中被忽略的方向。
    

再看输出。因为输出只有一个数 ，所以任何实数都可以被产生出来：

左零空间就是：

表示输出空间里没有不可达方向。

* * *

再看一个有“不可达输出”的例子。

设：

那么：

这个矩阵把一个数  变成二维向量 。所以输出只能落在 \-轴上。

因此：

-   ：能到达的输出。
    
-   ：\-轴方向永远到不了。
    

这时，如果方程是：

有解，因为右边在列空间中。

但如果：

无解，因为  永远产生不了第二个分量为  的输出。也就是说：

它有一部分落在左零空间方向上。

* * *

## 小结

我们可以用下面这个表格来总结：

位置

空间

它回答的问题

输入侧

行空间 

输入中，哪些方向会影响输出？

输入侧

零空间 

输入中，哪些方向被消掉？

输出侧

列空间 

哪些输出  可以由  得到？

输出侧

左零空间 

哪些输出方向永远得不到？

所以，对于 ，最核心的理解是：

-    **的行空间部分决定 ， 的零空间部分不影响 。**
    
-    **必须在列空间里，方程才有解；左零空间描述  中不可达的部分。**
    

希望这个视角能帮你真正理解“四大基本空间”存在的意义，而不是死记硬背定义。

## 推荐阅读

  

  

![](https://mmbiz.qpic.cn/sz_mmbiz_png/icVUVTFlBCVVKPaBk6IMGvUnuicNQJxIo3KEorLicIia2iclYzr6GQ6yLEomn1XIT3VHvMZ7wpNMJicuH41Dib23myUwQ/640?wx_fmt=png&from=appmsg&random=0.2312854885901674)

**闲鱼小店已上新，欢迎新老粉丝关注和咨询![](https://res.wx.qq.com/t/wx_fed/we-emoji/res/assets/Expression/Expression_64@2x.png)**

**喜欢****作者******，请点********赞********和在看******![](https://mmbiz.qpic.cn/mmbiz_gif/6BxCiaoAkhCpmWx6ic8Sg6X6PXsnJWo7a3hOSHEicIvYJO3nOPdf6GiaqmlM1gXnia8Ppu76xY0hiaTBXSjQNRsr6Dkg/640?wx_fmt=gif&wxfrom=5&wx_lazy=1&random=0.6459285066781566&random=0.9039664113437702&random=0.10616299980642463&random=0.37943585876598473&random=0.7246305284075434&random=0.1820491079508062&random=0.3136645327514245&random=0.8290227030922905&tp=webp)**

**![](https://mmbiz.qpic.cn/mmbiz_gif/pn1AOBGTwDVWGtThsMX1L4lk0EffmPg1BKAYaooibsaRwoYMzqxhJ1seOlZialOpryLocgAFt7Yb97uwwseMCLxA/640?wx_fmt=gif&wxfrom=5&wx_lazy=1&random=0.90499299240585&random=0.7427014598159356&random=0.6203706698189717&random=0.17266143983420013&random=0.4502076552242009&random=0.3348111146987862&random=0.5857982594073723&random=0.5924137199896844&tp=webp)**