在科学研究和工程应用中，常微分方程（ODEs）是描述动态系统行为的重要工具。然而，对于复杂的实际问题，解析解往往难以获得，因此数值求解成为一种不可或缺的方法。紧差分方法作为一种高效的数值解法，能够在不显著增加计算复杂度的前提下，有效地提高解的精度。本文将详细介绍紧差分方法的基本原理、实现步骤及其在两点边值问题中的具体应用。

##   
引言  

在前面介绍的一系列常微分方程的数值解法中，对于一个  阶收敛的算法，即数值解  与精确解  的误差满足  ，可以通过减小步长提升数值解的逼近效果。实际上，如果原来连续型方程的精确解有较高的正则性、方程在形式上也较为简单时，也可以通过其他方法，在保持步长不变的情况下提高精度。这里我们将介绍一种用于解决两点Dirichlet边值问题

的高精度方法——紧差分方法，其中  和  为已知函数，在  内  ， 为待求函数， 和  为常数。

##   
紧差分方法的基本原理  

为了构建差分格式，首先对求解区域  等距剖分为  份，得到  个节点 ， ，步长  。然后将原方程弱化，使之仅在节点成立，即有

对于内节点 ，选用**二阶差分格式**并**保留高阶项**，易见有

引入新变量  ，则  。因此，有

继续应用**二阶差分格式**，得到

由式(1)可得

因此有

我们忽略高阶小项，用数值解  代替精确解  ，简记 ， ，可得以下差分格式：

易见，此格式的局部截断误差为  。上式可整理为

这是一个由  个未知量  、  个方程构成的线性方程组，写成矩阵形式即为

其中

注意到  为三对角矩阵，因此，可以使用追赶法、高斯消元法或其他适当的算法求解该方程组，从而获得每个内部节点上的  值。

##   
例题  

对往期文章中的算例运用紧差分方法求解，具体问题为：

编写程序，分别计算  和  时 、 和  处的数值解，显示到小数点后 8 位，并将这些结果与精确解  比较。

###   
  
Fortran 实现  

利用 Fortran 语言，我们可以高效地实现该问题的数值解法。具体方法参见以下的代码和注释：

  
`program main   implicit none   integer:: m, i   real(8),parameter::Pi=3.14159265359d0   real(8):: a, b, h, alpha, beta   real(8),allocatable:: matrix_a(:), matrix_b(:),matrix_c(:), rhs(:), ans(:)   real(8),allocatable:: x(:), y(:)        m = 4 !总的剖分数   ! m = 8 !总的剖分数     a = 0.0d0 !边界左端点     b = Pi/2.0d0 !边界右端点     h = (b - a)/dble(m) !步长     alpha = 0.0d0 !左端点值     beta = 1.0d0 !右端点值      ! 节点坐标     allocate(x(0:m))     x =[(a + i * h, i =0, m)]      ! 矩阵的右端项，含m-1个元素的数组rhs     allocate(rhs(m-1))     rhs =[(h*h/12d0*(f(x(i-1))+10*f(x(i))+f(x(i+1))), i =1, m-1)]   ! 考虑边界条件     rhs(1)= rhs(1)- (1+h*h*q(x(0))/12d0)*alpha     rhs(m-1)= rhs(m-1)- (1+h*h*q(x(m))/12d0)*beta      ! 三对角矩阵，下次、主、上次对角线元素数组a、b、c，计算结果存储数组ans     allocate(matrix_a(m-1),matrix_b(m-1),matrix_c(m-1),ans(m-1))     matrix_a =[0d0,(1.0d0+h*h*q(x(i-1))/12d0, i =2, m-1)]       matrix_b =[(5d0*h*h/6d0*q(x(i))-2.0d0, i =1, m-1)]     matrix_c =[(1.0d0+h*h*q(x(i+1))/12d0, i =1, m-2),0d0]      ! 方程组求解     call chase_algorithm(matrix_a, matrix_b, matrix_c, m-1, rhs, ans)     deallocate(matrix_a, matrix_b, matrix_c, rhs)      ! y为数值解，组装计算结果     allocate(y(0:m))     y(0)= alpha     y(1:m-1)= ans     y(m)= beta     deallocate(ans)      ! 保存结果到文件     open(10,file="res.txt")     write(10,*)"#     x          y         yexact       err"     do i =0, m, m/4       write(10,"(4e15.8)") x(i), y(i), exact(x(i)),abs(exact(x(i))- y(i))     enddo     close(10)      contains   !原方程右端项函数f(x)   function f(x) result(res)   real(8):: res, x       res =-(x*x +1)*sin(x)   end function f   !原方程中的函数q(x)   function q(x) result(res)   real(8):: res, x       res =-x*x   end function q   !精确解   function exact(x) result(res)   real(8):: res, x       res =sin(x)   end function exact   !解决三对角方程组的追赶法程序   !a,b,c分别为系数矩阵的下次、主、上次对角线元素组   !n为方程组阶数，d为矩阵右端项向量   !ans为计算结果   subroutine chase_algorithm(a, b, c, n, d, ans)   integer,intent(in):: n   real(8),intent(in):: a(n), b(n), c(n), d(n)   real(8),intent(out):: ans(n)   real(8),allocatable:: g(:), w(:)   integer:: i   real(8):: p      allocate(g(n), w(n))      g(1)= d(1)/ b(1)   w(1)= c(1)/ b(1)      do i =2, n      p = b(i)- a(i)* w(i-1)      g(i)=(d(i)- a(i)* g(i-1))/ p      w(i)= c(i)/ p   enddo         ans(n)= g(n)   do i = n-1,1,-1       ans(i)= g(i)- w(i)* ans(i+1)   enddo      deallocate(g, w)   end subroutine chase_algorithm   end program main   `

在以上代码中分别取 `m=4` 和 `m=8` ，编译并运行两次，即可得到不同的计算结果文件`res.txt`。整理之后，将计算结果列表如下：


从上表可见，两组数值解之间呈现了理论预期的 4 阶精度，即步长减半后，后一列误差约为前一列误差的 1/16 ，充分验证了紧差分方法的精确性。

##   
小结  

紧差分方法是一种高效、高精度的数值解法，特别适用于两点边值问题的求解。它不仅能够提高数值解的精度，而且在计算成本上相对较低。通过对实例的具体分析，我们可以看到该方法在实际应用中的优越性能。

以上内容仅为概述，若需深入探讨紧差分方法的更多细节，如不同类型的边值条件、更高阶的差分格式以及实际应用案例，建议查阅相关专业文献或进行进一步的研究实践。希望读者通过本文，能对该方法有一个全面而深刻的理解，并在未来的工作中灵活运用。

  

往期推荐

[

步长选择与数值稳定性：常微分方程初值问题计算方法解析


](http://mp.weixin.qq.com/s?__biz=Mzk0MzI0NDU2NQ==&mid=2247490905&idx=1&sn=0dd5c95ef47c508ada48e34b93db623a&chksm=c3378923f4400035d82077ceabe640252a0a8ca37a3cb3f992803b5e589a666889c8c8384180&scene=21#wechat_redirect)

[

理查德森外推法：提高数值计算精度的利器


](http://mp.weixin.qq.com/s?__biz=Mzk0MzI0NDU2NQ==&mid=2247490174&idx=1&sn=c3f7006c8ace53678a90339a0adcfb2c&chksm=c3378e04f440071259268db65edc75d9efec183aa29f87095d24fa2fb10c9b58eb13b4572ac3&scene=21#wechat_redirect)

[

常微分方程两点边值问题的差分求解 | Dirichlet 边界条件


](http://mp.weixin.qq.com/s?__biz=Mzk0MzI0NDU2NQ==&mid=2247488303&idx=1&sn=d23842b7dcb7754ddecf7d328afe00d5&chksm=c3378755f4400e434f899d783fb160f9fdb746dea3c4da108b6007053c1dcfcc91117fb57f5a&scene=21#wechat_redirect)