# 线性代数共学：第 54 部分 - 特征值、特征向量与特征多项式


> 原文地址: [https://mp.weixin.qq.com/s/rJcFlC6W5orhlhIdR\_Wr5w](https://mp.weixin.qq.com/s/rJcFlC6W5orhlhIdR_Wr5w)

![](线性代数共学_第_54_部分___特征值_特征向量与特征多项式_images/img_000_f4b41e91a7e2.png)

这张图核心目标是回答一个问题：

> 给定一个方阵 ，怎样系统地找到它的特征值和特征向量？

* * *

## 1\. 方阵  表示一个线性映射

图中一开始写：

对应一个线性映射：

意思是：  
一个  方阵 ，可以看作把  中的向量变成另一个  向量的机器。

也就是：

* * *

## 2\. 特征值方程

图中给出：

这就是**特征值方程**。

它的意思是：

> 如果一个非零向量  经过矩阵  作用以后，方向没有改变，只是被拉伸、压缩或者反向，那么  就叫特征向量， 就叫对应的特征值。

例如：

表示  方向被放大 2 倍。

表示  方向不变。

* * *

## 3\. 最理想的坐标系：特征向量坐标系

图中说：

这里  是两个特征向量。

如果任意向量  都能写成：

那么：

由于  是线性映射，所以：

又因为：

所以：

这说明，在由特征向量组成的坐标系里，矩阵  的作用非常简单：

也就是说，矩阵只是在不同方向上分别进行伸缩。

这就是特征向量最重要的几何意义：

> 特征向量给出了矩阵最自然的伸缩方向。

* * *

## 4\. 如何寻找特征向量？

从特征值方程出发：

把右边移到左边：

因为：

所以：

提取 ：

因此：

图中写的是：

更准确地说：

也就是说：

> 特征向量就是矩阵  的零空间中的非零向量。

* * *

## 5\. 为什么要令行列式等于 0？

为了让：

有非零解，矩阵  必须是奇异矩阵。

如果  可逆，那么两边左乘逆矩阵：

就只能得到零解。

但特征向量要求：

所以必须有：

不可逆。

而方阵不可逆等价于行列式为 0：

因此：

非平凡是的特征值

这里“非平凡”就是指零空间里不仅有零向量，还有非零向量。

* * *

## 6\. 图中的具体例子

图中给出矩阵：

那么：

所以：

计算行列式：

对于  矩阵：

所以：

展开：

先算乘积：

再减去 2：

所以：

因式分解：

令它等于 0：

所以：

或者：

因此矩阵

的特征值是：

* * *

## 7\. 顺便把特征向量也算出来

### 对于 

要求：

设：

则：

所以：

取 ，得到：

所以  的一个特征向量是：

* * *

### 对于 

要求：

得到：

也就是：

取 ，得到：

所以  的一个特征向量是：

* * *

## 8\. 一般情形：特征多项式

对于一般的：

图中定义：

这个  就叫做矩阵  的**特征多项式**。

因为  的对角线元素会变成：

所以行列式展开之后，会得到一个关于  的  次多项式：

注意：有些教材定义特征多项式为：

而这张图使用的是：

两种定义只差一个符号因子：

但它们的零点完全一样，所以求出来的特征值相同。

* * *

## 9\. 本图最后的关键结论

图中最后写：

> The zeros of the characteristic polynomial are exactly the eigenvalues of .

翻译成中文就是：

> 特征多项式的零点，正好就是矩阵  的特征值。

也就是说：

的解，就是  的特征值。

其中：

所以求特征值的标准流程是：

* * *

## 10\. 总结成一句话

这张图的主线是：

表示特征向量方向不变，只被缩放。  
为了找到这样的方向，把它改写成：

要有非零解，就必须：

于是得到特征多项式：

它的零点就是特征值。  
找到特征值之后，再解：

就能找到对应的特征向量。

* * *

# 几何视角

图中这两张坐标图，表达的是同一个核心思想：

> 如果我们找到一组特征向量 ，并把它们当作坐标轴，那么矩阵  的作用就会变得非常简单：  
> 沿着  方向伸缩，沿着  方向伸缩，而不会把方向“搅混”。

* * *

## 1\. 左图：先把向量  分解到特征方向上

左边图里有三根主要箭头：

-   绿色箭头：特征向量 
    
-   红色箭头：特征向量 
    
-   黑色箭头：普通向量 
    

图中写：

这句话的几何意思是：

> 任意一个向量 ，都可以拆成两部分：  
> 一部分沿着  方向，另一部分沿着  方向。

就像在普通直角坐标系里，我们会写：

这里换了一套坐标轴，不再用标准坐标轴 ，而是用特征向量  当作新的坐标方向。

所以左图其实是在说：

在特征向量坐标系中可以表示为

也就是：

* * *

## 2\. 右图：矩阵  作用之后，各方向单独伸缩

右图展示的是  经过线性变换  之后的结果：

因为  是线性映射，所以可以拆开：

而  是特征向量，满足：

因此：

也就是：

几何意思非常直观：

> 原来  在  方向上的分量是 ，经过  后变成 ，被拉长 2 倍。  
> 原来  在  方向上的分量是 ，经过  后还是 ，长度不变。

所以右图中的黑色箭头 ，就是把左图中的  沿  方向拉长，而沿  方向保持不变之后得到的新向量。

* * *

## 3\. 两张图的关系：左图是输入，右图是输出

可以这样理解：

左图：输入向量

右图：输出向量

矩阵  就像一个“变形机器”：

左图中的  是变形前的向量。

右图中的  是变形后的向量。

但是关键点是：  
在特征向量方向上，变形非常干净。

沿  方向：

沿  方向：

所以整个平面被这样变形：

-    方向拉伸 2 倍；
    
-    方向保持不变；
    
-   所有其他方向的向量，由于可以拆成  两部分，所以也跟着被相应改变。
    

* * *

## 4\. 为什么这叫“optimal coordinate system”？

图中写了：

意思是“最理想的坐标系”。

为什么理想？

因为如果使用普通标准坐标系，矩阵  可能看起来很复杂，会把横向分量和纵向分量混在一起。

比如一般矩阵：

作用在向量上时，两个坐标往往会互相影响。

但是如果我们改用特征向量  作为坐标轴，那么矩阵  的作用变成：

这就非常简单了。

也就是说，在特征向量坐标系中， 就像一个对角矩阵：

它只做两件事：

方向乘以

方向乘以

没有旋转，没有混合，没有剪切。

这就是特征向量坐标系的强大之处。

* * *

## 5\. 可以把它想象成一张橡皮网格

左图中的网格可以看成一张橡皮网格。

绿色方向  是网格中的一个特殊方向，红色方向  是另一个特殊方向。

矩阵  作用之后：

-   沿绿色  方向，网格被拉长 2 倍；
    
-   沿红色  方向，网格不变；
    
-   斜着的向量 ，因为同时含有  分量和  分量，所以会被拉到新的位置，变成 。
    

所以左图和右图之间的关系可以理解为：

整张平面被沿特征方向重新拉伸

* * *

## 6\. 为什么普通向量  方向会改变，而特征向量方向不变？

这是理解这两张图的关键。

对于特征向量 ：

说明  变换后仍然在原来的直线上，只是长度变了。

对于特征向量 ：

说明  变换后也仍然在原来的直线上，甚至长度都没变。

但是对于一般向量：

变换后：

因为  分量和  分量的伸缩比例不同，所以合成出来的新方向通常会变。

也就是说：

> 特征向量方向保持不变；  
> 普通向量方向一般会改变。

这就是右图中黑色箭头  和左图中黑色箭头  方向不一样的原因。

* * *

## 7\. 用一句话概括两张图

左图：

表示把向量  分解到两个特征方向上。

右图：

表示矩阵  分别拉伸这些特征方向上的分量。

所以这两张图共同说明：

> 特征向量给出了矩阵最自然的变形方向；  
> 特征值告诉我们这些方向分别被拉伸或压缩了多少。