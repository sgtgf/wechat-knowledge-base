# 如何拆解一个系统（上）：构建核心关联


> 原文地址: [https://mp.weixin.qq.com/s/2HM4HhjnwDX7SibNhRXnEw](https://mp.weixin.qq.com/s/2HM4HhjnwDX7SibNhRXnEw)

# 如何拆解一个系统（上）：构建核心关联

## 心智锚点

你有一个  的矩阵 。它不是方阵，不对称，可能缺秩。

你想理解它的内在结构。特征分解不可用。

**SVD 的突破口在于：先构造两个对称矩阵  和 ，然后分析它们。**

## ：输入端的自相关

——第  列和第  列的点乘。

 度量了  的列之间的相似度。对角线是各列的能量（长度平方）。非对角线是列间的耦合度。

性质总结：

-   对称：。
    
-   半正定：。
    
-   正定（当  列满秩时）。
    
-   特征值 。
    

## ：输出端的自相关

 度量了  的第  行和第  行之间的相似度。

同样对称、半正定。

## 关键定理：非零特征值相同

和的非零特征值完全相同

设 （）。

令 。则：

 **是  的特征向量 →  是  的特征向量，对应同一个特征值 。**

两个方阵共享同一组非零特征值。差异只在于零特征值的个数（因为两个矩阵的尺寸可能不同）。

## 奇异值的定义

奇异值是 （或 ）的特征值的正平方根。

按惯例降序排列：，其中 。

## 从  到 SVD 的桥梁

 的特征分解给出了右奇异向量 。

 的特征分解给出了左奇异向量 。

奇异值  连接了两侧。

**SVD 不是凭空构造出来的。它是  和  这两个对称矩阵的特征分析自然汇聚的结果。**

## 为什么用  而不是 ？

因为  把  的所有结构信息"炼化"成了一个对称方阵——最好的矩阵类型。

-    可能不是方阵 →  一定是方阵。
    
-    可能不对称 →  一定对称。
    
-    可能几何意义不清 →  的几何意义明确（自相关/Gram 矩阵）。
    

## 现实映射

**构建关联思维：当面对一个结构不明的系统时，通过它与自身的"对话"（）来揭示内在结构。**

-   文本分析中的词-文档矩阵  是非方的。 给出了词与词之间的共现关系。 给出了文档与文档之间的相似度。
    
-   推荐系统中的用户-物品矩阵  是非方的。 揭示了物品之间的关联。 揭示了用户之间的关联。
    

 **和  是矩阵  的两面镜子——一面照输入，一面照输出。两面镜子合在一起，就是完整的 SVD。**