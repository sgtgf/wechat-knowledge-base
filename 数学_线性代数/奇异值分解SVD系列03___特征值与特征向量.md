# 奇异值分解SVD系列03---特征值与特征向量


> 原文地址: [https://mp.weixin.qq.com/s/QOrqxRBgA8IEO\_68MazeoA](https://mp.weixin.qq.com/s/QOrqxRBgA8IEO_68MazeoA)

**一、特征值与特征向量**

前面讲到，矩阵的本质是线性变换，Ax就是对空间内任一向量x作线性变换。一般而言，变换后的向量Ax与变换前的向量x朝向是不同的，但有一些向量很特别，**变换前后的朝向一致，只有大小有区别**，即Ax=λx，如下图中所示2个方向的向量，这两个向量就是特征向量，对应的缩放系数就是特征值。

![](奇异值分解SVD系列03___特征值与特征向量_images/img_000_789e3ecef4cb.png)

![](奇异值分解SVD系列03___特征值与特征向量_images/img_001_a9a74c2de477.png)

一个典型的应用场景，刚体在旋转的过程中，直观上感觉，总能找到一根不会动的轴，而这根轴的方向就是惯性矩阵特征向量的方向。  

![](奇异值分解SVD系列03___特征值与特征向量_images/img_002_4056d2f9b7fe.png)

**问题1**，如果变换矩阵是旋转矩阵，会有实特征值与特征向量吗？

**答案**：没有的，因为旋转矩阵会将每个向量旋转一个角度，从而无法保证变换前与变换后的向量朝向一致；  

**问题2**，如果变换矩阵是(图像)缩放矩阵，特征值与特征向量是什么？

**答案**：因为变换是缩放变换，故只是大小改变，方向不做变化，故一切向量都是特征向量，对应的缩放因子就是特征值

**二、特征分解**

那具体是怎么回事呢？简单而言，要线性变换v，可先将v表示成变换矩阵A的特征向量的线性组合，对v的变换就等价于这个线性组合的变换，而该组合中特征向量的变换，根据特征值与特征向量的定义，只是在大小上有缩放，而朝向不变，这样对v线性变换就简单多了。

![](奇异值分解SVD系列03___特征值与特征向量_images/img_003_2cd17ee44a92.png)

Av具体是这样实现的

**第1步**：将x表达成特征向量![](奇异值分解SVD系列03___特征值与特征向量_images/img_004_36fbe5e4e3dc.png)的线性组合(组合系数为v)，此时需对P矩阵求逆

![](奇异值分解SVD系列03___特征值与特征向量_images/img_005_f6f46851c986.png)

![](奇异值分解SVD系列03___特征值与特征向量_images/img_006_cf725b856f69.png)

**第2步**：将组合向量![](奇异值分解SVD系列03___特征值与特征向量_images/img_007_029c9ada5d53.png)按各轴的特征值进行拉伸缩放

![](奇异值分解SVD系列03___特征值与特征向量_images/img_008_7cdf05caf633.png)

**第3步**：将拉伸缩放的![](奇异值分解SVD系列03___特征值与特征向量_images/img_009_d9422c143b5d.png)重新组合

![](奇异值分解SVD系列03___特征值与特征向量_images/img_010_4e52b8bacb7e.png)

得到的特征分解如下：

![](奇异值分解SVD系列03___特征值与特征向量_images/img_011_a603404c6c75.png)

反向理解，将A特征分解后。其中P是归一化的特征向量组成的矩阵，F1为标准正交基表达的坐标系，F2为A的归一化的特征向量基表达的坐标系。那在F2下描述的缩放变换![](奇异值分解SVD系列03___特征值与特征向量_images/img_012_355e11160546.png)，对应于F1下是哪个矩阵呢？

若求F1下向量v的变换矩阵，可

首先，将F1下的向量v通过矩阵![](奇异值分解SVD系列03___特征值与特征向量_images/img_013_200163871686.png)变为F2下的向量表示x=![](奇异值分解SVD系列03___特征值与特征向量_images/img_014_950bd6cdd801.png)；

然后，用F2下的变换矩阵![](奇异值分解SVD系列03___特征值与特征向量_images/img_015_355e11160546.png)变换F2下的x，得![](奇异值分解SVD系列03___特征值与特征向量_images/img_016_f518af192876.png)；

最后，将F2下缩放后的向量通过P变回在F1下的表示,得![](奇异值分解SVD系列03___特征值与特征向量_images/img_017_2c802e4e0cd2.png)。

回答上面的问题，对应于F2下描述的缩放变换![](奇异值分解SVD系列03___特征值与特征向量_images/img_018_355e11160546.png)，在F1下就是![](奇异值分解SVD系列03___特征值与特征向量_images/img_019_c570165aa099.png)

**以上可看出，特征分解本质是采用了一种“曲线救国”的策略，即F1下看不出来的变换，则把F1下的向量转成可一眼看出来的F2下的缩放变换，然后再变回来。**

**即可对角化的变换矩阵，本质是在其特征向量组成的基坐标系下的缩放变换，即同一个变换在不同坐标系下的描述不同而已，在标准正交基下是A,在其正交特征向量基下是**![](奇异值分解SVD系列03___特征值与特征向量_images/img_020_355e11160546.png)**。**