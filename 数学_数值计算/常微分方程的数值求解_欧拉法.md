# 常微分方程的数值求解：欧拉法


> 原文地址: [https://mp.weixin.qq.com/s/Whcan-ye-r2XKIMn4WbIdQ](https://mp.weixin.qq.com/s/Whcan-ye-r2XKIMn4WbIdQ)

![](https://mmbiz.qpic.cn/mmbiz_gif/VIM0SmWQpWvsQIEAS5b228jQGB4FurcBibs2iaubicTaW5IkOa3r4XpLaPyzmen89DAcSJr1ObOucT9iceic0xehFcg/640?&random=0.7208003402280347&random=0.16634114621985852&random=0.8658017081419105)

点击上方「蓝字」关注我们

![](https://mmbiz.qpic.cn/mmbiz_png/2MhiaCtgicxW6hVicfNzL5yE9b4tViaWuriaVsWhgzqbAxD0eIYhziaLtLI1uwLENGob23fQJEVe1M77eMPRP8yNHrKQ/640?&random=0.5083455645781676&random=0.6902606942797083&random=0.5006165306553911)

在科学计算和工程应用中，常微分方程（ODE）是描述各种物理现象的基础工具之一。然而，并非所有常微分方程都能找到精确解析解，因此数值方法成为了解决这类问题的重要手段。本文将介绍一种常用的数值求解方法——欧拉法，深入探讨其原理、应用以及局限性，旨在为读者提供一个理解和应用这些方法的框架。

## 一阶常微分方程初值问题

考虑一个简单的一阶常微分方程 及其初始条件 ，其中 是定义在某个区域 上的连续函数。为了确保该方程存在唯一解并且这个解连续依赖于初始条件 ，我们要求 满足Lipschitz条件，即存在正数 ，使得对于所有 和任意两个不同的 ，有：

此条件保证了问题的良好性质，使得我们可以安全地应用数值方法来寻找近似解。

## 欧拉法简介

### 方法概述

欧拉法是一种直观而简单的数值求解策略，它基于泰勒级数展开的思想，通过逐步逼近的方式来估计未知函数 的值。具体来说，我们将时间区间 分成 等份，每部分长度 称为步长， 表示第 步的时间点。从已知的初始值出发，利用给定的微分方程计算出下一个时刻的状态变化量，从而迭代更新 的值。

根据上述思想，欧拉法的核心递推公式可以写作：

这意味着，在每个时间步上，我们使用当前点处的斜率乘以步长作为增量，以此预测下一刻的位置。

![](https://mmbiz.qpic.cn/sz_mmbiz_png/icVUVTFlBCVVvIrUQn74JaQVbItjVNyJccVyVnVnSeh9HGtm3DaTJN3QqbG3P8aBicnxaqolyDkl2kjF85ln6YtQ/640?wx_fmt=other&from=appmsg&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1&random=0.8603032766847831&random=0.19227240962900738&random=0.8955936592663003&random=0.8764700001631964)

### 推导过程

为了更好地理解欧拉法的工作机制，下面我们将分别从泰勒展开和数值积分的角度进行解释。

#### 泰勒展开

假设 在 处具有二阶可导性，则根据泰勒定理，可以在 附近写出如下表达式：

忽略高阶项后，得到近似公式：

这正是欧拉法的基础形式，它用线性近似代替了真实的曲线变化。

以此类推，即可得到递推公式：

#### 数值积分

另一种看待欧拉法的方式是将其视为对原微分方程进行离散化后的数值积分。我们知道，理论上 可以表示为：

特别地，当 时，

如果采用左矩形法则近似积分，则可得：

这与之前通过泰勒展开获得的结果是一致的，证明了两种推导方式之间的内在联系。

### Python实现

以 为例，其精确解为 ，下面是用Python实现欧拉法的代码：

`import math   from matplotlib import pyplot as plt      t_0 = 0   y_0 = 1   tau = 0.1   i = 1   solve = []   Euler = []   t = []   while i < 100:       if i == 1:           y_n = y_0           t_n = t_0       Euler.append(y_n)       solve.append(math.exp(t_n))       t.append(t_n)       func = y_n       y_n = y_n + tau * func       t_n = t_n + tau       i += 1      plt.plot(t, Euler, c='green', label=' Euler method')   plt.plot(t, solve, c='red', label=' accuracy')   plt.fill_between(t, solve, Euler, facecolor='blue', alpha=0.2)   plt.title('Euler method', fontsize=19)   plt.xlabel('t', fontsize=19)   plt.ylabel('y', fontsize=19)   plt.legend()   plt.show()   `

计算结果为：

![](https://mmbiz.qpic.cn/sz_mmbiz_png/eRETdLuoXm0jM872M2RkEQdf1cUvIYlZcYql0IdF3OGiczJYz404YOcbVyl1Dvic02icl2W3PLvcCd8cgJrk97d6A/640?wx_fmt=png&from=appmsg&random=0.8933849150332696&random=0.09576755852969954&random=0.2779279268038113&random=0.012396579580935763)

### 误差分析

从上图可以看到，当迭代步数较多后，欧拉法的结果逐渐落后于精确指数解的增长速度。下面分析欧拉法的误差来源。

在  中，略去了高阶小量

因此，在每一步的递推中，都有局部截断误差 ，其阶为 。

在计算中，我们更关心精确解和数值解之间的误差 ，称为整体误差，其满足

根据 Lipschitz 条件，可得：

由 且 ， ，可得：

局部截断误差 是 的二阶量，设 ，得： ，整体误差是一阶量 。

### 稳定性分析

如果计算的初值不能精确给定，例如存在测量、舍入误差等，在计算过程中，每一步传递的误差连续依赖于初始误差，则称算法稳定，否则该算法不稳定。

对于不同的初值 和 ，有

两式相减，得：

根据 Lipschitz 条件，可得：

连续依赖于初始误差，欧拉法稳定。

## 结语

综上所述，欧拉法作为一种经典的数值求解技术，以其简洁性和易用性赢得了广泛的应用。尽管它不是最精确的方法，但在很多情况下已经足够满足需求，特别是当我们追求快速估算而非极致精度时。希望本文能帮助你深入了解欧拉法背后的数学原理，并启发你在实际工作中灵活运用这些知识。

# 推荐阅读

  

![](https://mmbiz.qpic.cn/mmbiz_gif/DRPKCGMAPjKzncOsqKVQInBQ7RU7WTSXJdmD2OEMiciawJtrbZAicXnwK8cFHw5ddlCRcHWKu2ZxvHlpHmU8UN4Ew/640?wx_fmt=gif&random=0.5314540185644026&random=0.712705662344495)

  

**喜欢****作者******，请点********赞********和在看******![](https://mmbiz.qpic.cn/mmbiz_gif/6BxCiaoAkhCpmWx6ic8Sg6X6PXsnJWo7a3hOSHEicIvYJO3nOPdf6GiaqmlM1gXnia8Ppu76xY0hiaTBXSjQNRsr6Dkg/640?wx_fmt=gif&wxfrom=5&wx_lazy=1&tp=wxpic)**

**![](https://mmbiz.qpic.cn/mmbiz_gif/pn1AOBGTwDVWGtThsMX1L4lk0EffmPg1BKAYaooibsaRwoYMzqxhJ1seOlZialOpryLocgAFt7Yb97uwwseMCLxA/640?wx_fmt=gif&wxfrom=5&wx_lazy=1&tp=wxpic)**