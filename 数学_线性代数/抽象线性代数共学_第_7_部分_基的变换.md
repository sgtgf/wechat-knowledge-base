# 抽象线性代数共学：第 7 部分-基的变换


> 原文地址: [https://mp.weixin.qq.com/s/E8yIrwXyNrqJMZX4vRiuOA](https://mp.weixin.qq.com/s/E8yIrwXyNrqJMZX4vRiuOA)

![](https://mmbiz.qpic.cn/mmbiz_png/sVzeakRoocv4mY40aaT05bhx0ktdOqrJqlrGV0p7jicZrMkxbQoz1R7TqvUibhElfnpm6MTickia51Js1PmcsUtLmgibDicPHFDetAqCODWvic0xqk/640?wx_fmt=png&from=appmsg)

这张图讲的是抽象线性代数里“坐标”和“换基”的核心思想。

一句话概括就是：

**同一个向量 ，在不同基下，会有不同的坐标列向量；而“换基”就是把一种坐标表示，线性地变成另一种坐标表示。**

* * *

## 1\. 图最上面在讲什么

图中先给了一个向量空间 ，并且在  里选了两组基：

也就是说，**同一个空间** ，我们可以用两套不同的“参考坐标系”去描述里面的向量。

* * *

## 2\. 左右两个  是什么意思

图里从  分别指向两个 ，对应两张“坐标地图”：

这里的意思是：

-   选定基  后，每个向量  都能唯一写成
    
    所以它对应的坐标就是
    
-   同理，若用基 ，则
    
    对应坐标为
    

所以图里写的 **basis isomorphism（基诱导的同构）** 的意思就是：

> 一旦选定一组基，抽象向量空间  就和具体的坐标空间  一一对应起来了。

* * *

## 3\. 图中 “Recall” 部分在回顾什么

图里回顾了：

这里  是  的标准基向量，比如

这句话非常重要，意思是：

-   在基  下，
    
-   基向量  的坐标，
    
-   就是第  个标准基向量 。
    

因为  本来就等于

所以它的坐标当然是第  位为 1，其余位为 0。

图里还写了逆映射：

意思是：  
在坐标空间里拿到标准基向量 ，再通过逆映射回去，就得到原空间里的基向量 。

所以这两张图其实是在说：

-   ：把“抽象向量”变成“坐标列”
    
-   ：把“坐标列”还原成“抽象向量”
    

* * *

## 4\. “For each ” 那一行的含义

图里写：

这表示：

> 一个向量本身，可以看成“它的坐标列向量”经过逆坐标映射还原出来的结果。

也就是说，向量和坐标不是两回事，而是**同一个对象的两种看法**：

-   在  中看，它是向量 
    
-   在  中看，它是坐标列向量
    

* * *

## 5\. 中间的多项式例子

图中例子取的是多项式空间：

这表示所有次数不超过 2 的实系数多项式组成的向量空间。

### 第一组基

图中先取

其中

这就是最常见的“标准基”。

现在给一个多项式：

把它写成基  的线性组合：

所以在基  下，它的坐标是

这就是图中绿色括号里的 coordinate vector。

* * *

## 6\. 为什么换一组基，坐标会变

图里又选了另一组基 ，其中大致是：

这时候同一个多项式

可以改写成

所以它在新基  下的坐标就变成

注意这里特别关键：

-   **向量  没有变**
    
-   变的是**描述它的基**
    
-   所以变的是**坐标**
    

这就是“同一个人，换了不同地图上的地址写法”。

* * *

## 7\. 图中 “old vs. new coordinates” 在说明什么

图里左边写的是旧坐标：

并且

右边写的是新坐标：

并且

这说明：

同一个 ，既可以写成  的线性组合，也可以写成  的线性组合。  
因此它就有两套坐标：

-   旧坐标 
    
-   新坐标 
    

* * *

## 8\. 最下面的图：换基函数到底是什么

图的核心公式是：

它表示**把 \-坐标变成 \-坐标**。

过程分两步：

### 第一步：先从旧坐标回到向量

如果你手里有一个旧坐标 ，它其实是某个向量  在基  下的坐标。  
所以先通过

把它还原为真正的向量 。

### 第二步：再从向量变成新坐标

然后再通过

把这个向量送到新基下的坐标。

所以换基映射就是：

也就是图中最后那行：

* * *

## 9\. 为什么它一定是线性的

图里右下角特地写了：**linear!**

原因很简单：

-    是线性映射
    
-    也是线性映射
    
-   两个线性映射复合以后还是线性映射
    

所以换基绝不是“随便变一下”，而是一个**线性变换**。  
这意味着它一定可以用一个矩阵表示。

* * *

## 10\. 这张图真正想让你记住的结论

这张图最重要的思想有 4 个：

### 结论 1：选一组基，就相当于给向量空间装了一个坐标系统

* * *

### 结论 2：坐标依赖于基

同一个向量，在不同基下，坐标通常不同。

* * *

### 结论 3：换基就是“旧坐标  新坐标”的线性映射

* * *

### 结论 4：抽象向量问题可以转化成矩阵问题

这正是线性代数里非常强大的地方：

-   在抽象空间里研究向量、线性映射
    
-   选基后，把它们变成坐标列向量和矩阵
    
-   然后用矩阵计算来处理
    

* * *

## 11\. 你可以把整张图理解成一个流程

**向量**   
   
**旧坐标** 

或者

**向量**   
   
**新坐标** 

而“换基”就是：

其公式正是

* * *

## 12\. 用这张图里的例子再说一遍

对于

在基

下：

在另一组基

下：

所以换基做的事就是把

变成

但注意，本质上它们描述的是**同一个多项式**。

* * *

我们做一个**正式一点的二维换基例子**：

# 例子：两组任意基之间的换基

设向量空间还是

取两组基：

其中

以及

这两组都是线性无关的，所以都是  的基。

* * *

# 1\. 先给一个向量的旧坐标

假设某个向量  在基  下的坐标是

这句话的意思不是说 ，而是说

把  代进去：

所以这个向量其实是

* * *

# 2\. 现在要求它在新基  下的坐标

我们要找 ，使得

也就是

右边合并：

于是得到方程组：

两式相减：

再代回去：

所以

* * *

# 3\. 这就是一次换基

我们已经完成了：

注意：

-   旧坐标是相对于基  的
    
-   新坐标是相对于基  的
    
-   中间那个真正的向量  没变
    

* * *

# 4\. 用矩阵方法来做，更像正式写法

在抽象线性代数里，更常见的是用矩阵统一处理。

* * *

## 第一步：写出两组基对应的“基矩阵”

把基向量按列排成矩阵：

因为它的两列分别是 。

同理，

它的两列分别是 。

* * *

## 第二步：理解它们各自的作用

对于任意向量 ，有

以及

因为“基矩阵  坐标列 = 实际向量”。

* * *

## 第三步：把旧坐标变成新坐标

由于

又因为

代入得

这就是**从 \-坐标到 \-坐标的换基矩阵**。

* * *

# 5\. 具体算出这个换基矩阵

先求

因为

其行列式为

所以

因此换基矩阵为

计算得：

* * *

# 6\. 用这个矩阵直接换坐标

现在直接算：

得到

和前面手工解方程得到的结果完全一致。

* * *

# 7\. 这个矩阵每一列其实是什么意思

这一点非常重要。

换基矩阵

它的第 1 列，就是  在基  下的坐标。  
它的第 2 列，就是  在基  下的坐标。

我们验证一下。

* * *

##  在  下的坐标

设

即

得到

相减得 ，再得 。

所以

这正是换基矩阵的第一列。

* * *

##  在  下的坐标

设

即

得到

相减得 ，再得 。

所以

这正是换基矩阵的第二列。

* * *

# 8\. 所以为什么矩阵乘法有效

因为

表示

而在  坐标下，

也就是

这正是“用换基矩阵乘旧坐标”的本质。

* * *

# 9\. 总结成一个通用公式

若

那么从 \-坐标到 \-坐标的换基矩阵是

因此

这就是二维情形的正式写法；在  维时完全一样。

* * *

# 10\. 这题最值得记住的三个点

第一，  
**同一个向量**  可以有不同坐标：

第二，  
换基不是换向量，而是换描述方式。

第三，  
换基矩阵

的每一列，就是旧基向量在新基下的坐标。